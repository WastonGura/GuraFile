# Changelog

## 0.2.0 — Alpha

### 自动分类

- 按扩展名生成粗粒度类型和具体格式自动标签，并用最多 32 字节文件头识别常见图片、音视频、文档和压缩格式。
- 扩展名与文件头冲突时显示“类型冲突”，不会修改真实文件；读取失败保留扩展名分类且不阻塞索引。
- 新增或元数据变化时自动重算，只替换自动标签；用户标签保持不变，也可手动重新识别单个在线文件。

### 实时一致性

- 使用 FileSystemWatcher 跟踪外部创建、修改、删除、重命名和移动，并在 200 ms 窗口合并事件后回读磁盘状态。
- 同卷重命名、移动和扩展名变化保持文件节点及用户标签，自动格式标签随真实文件状态更新。
- 应用启动时异步执行补偿扫描；监听错误或丢失事件会合并为受控全根恢复扫描。

### 离线与恢复

- 根目录显示在线、离线或恢复中状态以及最近错误。
- 根目录不可访问时保留已有节点和标签；重新上线后自动扫描并恢复监听。
- 恢复扫描前先重建 watcher，扫描期间的新变更仍会进入定向对账，不留下监听空窗。

### 升级与风险

- v0.1.0 数据库会自动迁移到 schema v5；升级前仍建议导出用户标签 JSON，旧版应用不能打开迁移后的数据库。
- 实时监听基于 FileSystemWatcher，并以启动/错误恢复扫描兜底；本版不读取 NTFS USN Journal。
- 发布包未签名，仅支持 Windows x64；仍不提供文件写操作和图谱视图。

## 0.1.0 — Technical Preview

### 标签式文件管理

- 扫描用户选择的根目录并建立本地 SQLite 索引。
- 使用稳定 Windows 文件身份保留同卷重命名和移动后的标签关系。
- 提供虚拟化文件列表、元数据搜索、排序和多选。
- 支持用户标签 CRUD、批量贴标以及 Any/All 组合筛选。

### 文件与数据安全

- 使用系统默认应用打开文件，并通过原生 Windows Shell API 在资源管理器中定位。
- 导出和恢复版本化用户标签 JSON；自动标签和可重建元数据不进入备份。
- 导入前验证格式、版本、大小和结构，数据库写入在单个事务中完成。

### 升级与风险

- 这是首个公开预览版，没有旧版应用升级要求。
- SQLite schema 会自动向前迁移；降级到不支持当前 schema 的版本会被拒绝。
- 发布包未签名，仅支持 Windows x64。使用前请核对 SHA-256。
- 已知限制见 README：没有实时监听、文件写操作和图谱。
