# Changelog

## 0.5.0 — Beta

### 用户标签自动滚动备份与恢复入口（#71）

- 引入滚动标签备份机制（`RollingTagBackupService`），在用户标签创建、重命名、删除以及文件贴标/移除标签后自动导出标签快照至 `%LOCALAPPDATA%\GuraFile\backups\tags\`；
- 每日最多保留一份快照，无变化跳过写入，自动滚动保留最近 14 份历史快照并清理最旧备份；
- 提供历史备份列表浏览、格式与损坏检测及单事务还原入口，备份写入失败不阻塞核心打标操作。

### 历史数据库逐级迁移矩阵（#72）

- 建立从 v1 (v0.1.0) 至 v10 的全自动化数据库向前迁移矩阵与验证用例；
- 严格在单个事务内逐版本迁移，校验外键与 WAL 模式，发生异常时原子回滚，保障升级过程历史用户数据与标签零丢失。

### 数据库损坏检测与安全重建（#73）

- 引入数据库健康度检测机制（`DatabaseHealthService`），准确识别 `malformed` 或损坏的 SQLite 数据库；
- 提供安全隔离与重建工作流（`DatabaseRecoveryService`）：经用户确认后自动将损坏数据库及 WAL/SHM 隔离备份至 `.corrupt_timestamp.bak`；
- 初始化全新数据库并从管理根目录重新扫描文件索引，随后自动从最新有效滚动备份恢复用户标签，输出详细对账报告。

### 本地诊断日志与用户主动导出（#74）

- 引入结构化分级本地日志（`DiagnosticLogger`），记录关键文件操作、对账、扫描、备份与异常信息，日志单文件上限 10 MB 自动轮转；
- 提供脱敏与诊断包导出服务（`DiagnosticExportService`），一键打包当前日志、数据库健康状态、根目录配置至 ZIP，自动脱敏用户路径与敏感标识。

### 明确网络盘、可移动介质与重解析点降级状态（#75）

- 细化不同存储介质的能力检测（`StorageCapabilityService`），精准区分本地 NTFS、可移动盘（FAT32/exFAT）、网络共享（UNC/SMB）与只读卷；
- 对不支持稳定身份或缺少实时监听的介质在界面提供明确的降级提示与只读警示，防误删、防越界。

### 崩溃后恢复未完成扫描（#76）

- 数据库引入 `scan_sessions` 表与状态机持久化扫描会话（`running`、`completed`、`interrupted`）；
- 应用非正常退出或崩溃重启后，自动检测未完成的扫描任务并触发补偿对账扫描，确保索引与真实磁盘文件状态完全同步。

### 防止崩溃后重复执行文件写操作（#77）

- 引入文件写操作意图持久化模型（`file_operation_intents` 与 `file_operation_intent_items`）；
- 在调用系统 Shell 物理写操作前记录意图；崩溃重启后仅对账磁盘状态并更新索引状态，绝对不重放物理写操作，彻底杜绝重复移动/重命名/删除风险。

### 十万文件扫描与冷启动性能优化（#78）

- 优化 SQLite 索引结构，为 `files(name COLLATE NOCASE)` 建立前缀检索专用索引，消除查询时的 Temp B-Tree 排序瓶颈；
- 引入快速前缀范围检索替代全表模糊扫描，实现 10 万文件冷启动查询约 1.0 秒、后台扫描吞吐达 4,000+ files/s。

### 使用 FTS5 优化搜索筛选与千文件批量标签（#79）

- 引入 SQLite FTS5 全文检索引擎与虚拟表 `files_fts`，建立高效的触发器同步机制；
- 建立安全词元转义构建器（`FtsQueryBuilder`），实现 10 万级别文件毫秒级文件名与路径全文检索；
- 优化批量打标事务写入，实现 1000 个文件批量贴标耗时收敛至 10 ms 级。

### 已保存筛选视图（#80）

- 建立已保存筛选视图持久化（`saved_filter_views` 与 `saved_filter_view_tags`）；
- 支持保存搜索词、排序方式、标签组合及匹配模式；标签按 Unicode 规范化并使用稳定 ID 关联；
- 当关联标签被用户删除时，视图自动标识 `[失效]` 并采用 `0 = 1` 边界保护，配备代际门禁防并发覆盖与事件死循环保护。

### 状态栏、焦点管理、基础无障碍与最小设置页（#81）

- 实现 `UserSettingsService` 容错持久化与开箱即用的最小设置页；
- 编排主窗口完整 TabIndex 键盘导航链路，消除折叠面板焦点陷阱；
- 为关键控件与状态栏接入 `AutomationProperties` 与 `LiveSetting.Polite` 无障碍播报；
- 强化剪贴板并发防护，消除 `CLIPBRD_E_CANT_OPEN` 瞬态锁屏。

### Beta 端到端全链路验收与发布准备（#82）

- 补齐“管理根目录 -> 扫描 -> 贴标 -> FTS5 搜索 -> 创建保存视图 -> 复制/重命名/移动 -> 回收站删除 -> 模拟崩溃及意图对账 -> 重启恢复验证”全流程端到端自动化验收；
- 补齐 v1 (v0.1.0) 至 v10 (v0.5.0) 数据库逐级迁移端到端验收与用户标签无损检验；
- 补齐数据库损坏/清空后从磁盘重新扫描、从滚动标签备份恢复并报告冲突与未匹配文件的端到端验收；
- 全工程版本号升级至 0.5.0（0.5.0.0），校验打包脚本、发布清单、文档与第三方许可一致性。

### 升级与风险

- 数据库 Schema 升级至 v10（包含 FTS5、扫描会话、操作意图与已保存视图等）；
- 支持历史任意版本（v0.1.0 ~ v0.4.1，即 Schema v1 ~ v9）数据库自动安全向前迁移；
- 升级前仍推荐使用“导出备份”或“历史备份...”保存一份 JSON 用户标签备份；
- 发布包继续保持自包含、未签名的 Windows x64 ZIP，不提供永久删除。

## 0.4.1 — Patch

### 真实图谱首帧性能修复（#67）

- 更正 v0.4.0 的性能验收：发布后使用真实 WebView2/Cytoscape.js `layoutstop` 复验，310 节点 / 300 边三次 JS 首帧为 1401.10 / 1404.30 / 1424.60 ms，未达到三次均小于 1000 ms 的门禁。
- 保留现有 Cytoscape.js、离线宿主、图模型和交互，仅将 `cose` 布局限制为 `numIter: 400`；加入隔离 WebView2 profile 的真实可见窗口 harness，不读取或修改用户 `index.db`。
- 最终目标设备连续三次 JS 为 668.1 / 670.0 / 669.4 ms，Host 为 729.88 / 725.11 / 721.67 ms；每次均为 310 节点 / 300 边、零远程请求且四项图谱资产哈希一致。
- 测量环境为 Windows 10.0.26200.0、AMD Ryzen 7 7735H、WebView2 152.0.4191.62、.NET 10.0.11、1280×800 可见窗口；结果仅适用于此目标 x64 设备范围，其他设备需单独复验。

### 发布准备（#68）

- 将产品、程序集、文件版本、README 资产名与命令、第三方 notices、打包默认值和发布清单统一更新为 0.4.1。
- 继续提供未签名、unpackaged、自包含 `win-x64` ZIP 与独立 SHA-256，不修改既有 v0.4.0 tag 或 Release 资产。
- 原生兼容 v0.2.0、v0.3.x 和 v0.4.0 schema v5 数据库；升级无须数据迁移，建议在升级前导出用户标签 JSON。

## 0.4.0 — Feature

### 受限二部图快照生成（#57）

- 实现 `GraphSnapshotService` 生成确定性受限文件—标签二部图快照（仅 `file:*` 到 `tag:*` 边，绝不产生两两文件边）。
- 严格限制 300 个文件节点上限；当文件数超过 300 时返回明确 `FileLimitExceeded` 状态且不截断渲染。
- 默认排除宽泛 `类型/*` 自动标签，并支持按需包含并标记 `isBroad`。

### 本地 Cytoscape.js 安全图谱宿主（#58）

- 随包完整分发完全离线的本地 Cytoscape.js 3.30.2、HTML、CSS 与 JS，断网环境完全可用。
- 在 WinUI 3 原生 WebView2 中映射虚拟域名 `graph.gurafile.local`，实施外部导航、新窗口弹窗、文件下载三重安全拦截。
- 实施严格 Content Security Policy（CSP），引号、换行、HTML/脚本文本在图谱快照与消息中安全作为纯文本保留，杜绝脚本注入。
- 版本化结构化 JSON 双向通信协议（`renderSnapshot`、`ready`、`firstFrameRendered`、`fitViewport` 等），节点按粗粒度类型着色，标签大小反映度数。
- 完整支持加载中、空图、超限提示（>300 个文件提示收窄筛选）与错误提示等界面状态。

### 列表与图谱筛选选择联动及安全打开（#59）

- 列表视图与图谱视图共享完全相同的 `_currentFiles` 筛选快照，保持文件集合始终一致。
- 建立 `GraphInteractionCoordinator` 实现 query/graph 双 generation 门禁，消除快速筛选切换时的竞态与过期快照回写。
- 单击文件节点同步列表高亮并刷新右侧文件详情面板；单击标签节点展示标签类型摘要且不触发文件操作。
- 双击文件节点仅通过内存中已验证的当前在线文件 ID 安全调用 `ShellFileActions.Open`，严格拒绝任意 Web 路径、离线文件与未知节点。
- 视图切换在“列表”（默认）与“图谱”间平滑过渡，保留全部筛选条件与选中项。

### 图谱框选与批量用户标签（#60）

- 在图谱视图中启用原生框选功能（`boxSelectionEnabled: true`），严格过滤仅选中文件节点，标签节点不进入写入集合。
- 宿主与图谱通过 `selectionChanged` 与 `setSelection` 双向同步选中状态，使用互斥标识防止事件回环。
- 多选文件时展示共有用户标签摘要，批量打标与去标均在单个 SQLite 事务中原子执行，失败完全回滚。
- 严格限制批量打标仅针对用户标签，自动标签绝对只读保护。

### Graph Preview 验收与发布（#61）

- 固化 300 文件节点及边快照生成与 JSON 序列化基线；发布后确认该检查不代表真实 WebView2 首帧，真实 `layoutstop` 三次为 1401.10 / 1404.30 / 1424.60 ms，未达到 1 秒门禁，已由 v0.4.1 修复。
- 覆盖快速连续切换筛选无旧节点回写、1000 个同标签文件绝不产生文件—文件边。
- 验证恶意文件名/标签文本纯数据保留、断网离线资源完整性、严格 CSP 与三重安全拦截。
- 验证批量标签单事务失败全部回滚、成功后两视图同步。
- 打包脚本强制验证离线图谱资源完整性与第三方许可 notices。

### 升级与风险

- 原生兼容 v0.2.0、v0.3.0、v0.3.1 与 v0.3.2 schema v5 数据库；升级无须数据迁移，建议在升级前导出用户标签 JSON。
- 图谱预览功能采用 300 文件节点上限保护渲染性能；超过 300 个文件时请使用左侧条件收窄筛选。
- 发布包未签名，仅支持 Windows x64；不提供文件永久删除选项。

## 0.3.2 — Patch

### 回收站测试产物精准清理（#53）

- 真实实现回收站测试产物精准清理：在 `RecycleBinTestHelper` 中实现 `CleanupRecycleBinItem` 与 `CleanupRecycleBinItemsForDirectory`，通过 STA Shell COM 精准匹配测试目录与 GUID 文件名，调用 `undelete` 恢复至原路径后物理删除。
- 绝不弹窗、绝不挂起无头 STA 线程，绝不触碰非本次测试的历史项目。
- 在 `TestEnvironment.Dispose()` 与 `TempDirectory.Dispose()` 中自动触发精准清理，保证相关测试运行后无 GUID 测试残留。

### 真实 Shell 失败路径端到端防护验证（#53）

- 补齐真实 Shell / 回收站失败路径端到端测试：在 `SafeFileOperationExecutorTests` 中排他锁定文件调用真实 `DeleteToRecycleBinAsync`，实测 Shell 报告失败且源文件物理存在、内容未被破坏或截断。
- 在 `FileOperationIndexCommitterTests` 中实测排他锁定文件执行删除时，源文件保留、数据库节点保持在线状态（`is_online = 1`）且已有用户标签不受损。

### 升级与风险

- 原生兼容 v0.2.0、v0.3.0 与 v0.3.1 schema v5 数据库；升级无须数据迁移，建议在升级前导出用户标签 JSON。
- 本版本取代 v0.3.1，彻底解决测试产物残留问题并补齐端到端失败路径防护实测。
- 发布包未签名，仅支持 Windows x64；不包含永久删除或图谱视图。

## 0.3.1 — Patch

### 回收站安全契约修复（#47）

- 修复删除到回收站的安全契约：在 Windows 原生 `IFileOperation` 中显式设置 `FOFX_RECYCLEONDELETE` 标志，与 `FOF_ALLOWUNDO` 协同保证只执行回收站软删除。
- 严禁降级为永久删除：若回收站不可用或 Shell 拒绝删除，操作立即安全失败，真实文件原位保留，索引和已有用户标签完整保留。

### 测试隔离与端到端业务链验收（#48）

- 回收站测试隔离：建立隔离的真实回收站端到端测试，精确识别并清理测试自身产生的一系列回收站项目，杜绝测试污染与残留。
- 补齐“扫描 → 贴标 → 复制 → 移动 → 重命名 → 回收站软删除 → 重启恢复”完整端到端业务链验证。

### 升级与风险

- 原生兼容 v0.2.0 与 v0.3.0 schema v5 数据库；升级无须数据迁移，建议在升级前导出用户标签 JSON。
- 本版本取代 v0.3.0；修复了 v0.3.0 中缺少 `FOFX_RECYCLEONDELETE` 可能导致意外永久删除的安全隐患。
- 发布包未签名，仅支持 Windows x64；不包含永久删除或图谱视图。

## 0.3.0 — Alpha

### 安全文件操作与标签继承

- 建立基于 Windows STA `IFileOperation` 的安全文件操作执行器，支持批量复制、移动与单文件重命名，并提供自动重命名、覆盖和跳过三种冲突策略。
- 同卷移动与重命名保留稳定文件身份与已有全部用户标签；复制与跨卷移动为新文件显式继承用户标签并重新计算自动类型标签。
- 跨卷移动若源文件清理失败则主动保护源节点及标签；提交前重新校验目标文件真实身份防止外部替换。

### 文件列表操作与资源管理器互通

- 建立文件剪贴板服务，采用 Windows 标准 `CF_HDROP` 与 `Preferred DropEffect` 格式，支持与 Windows 资源管理器双向复制/剪切/粘贴。
- 支持从 Windows 资源管理器拖入文件复制到在线管理根目录，以及在应用内将所选文件拖放到目标根目录执行移动；文件夹拖入安全拒绝，同位置判定为无操作。
- 接入常用文件操作键盘快捷键（`Ctrl+C`、`Ctrl+X`、`Ctrl+V`、`Ctrl+A`、`F2`、`F5`、`Delete`），文本框编辑时完全不拦截文本快捷键。

### 回收站安全删除与详情面板

- 实现删除到 Windows 回收站（`FOF_ALLOWUNDO`），严禁任何永久删除回退；明确拦截 `Shift+Delete`。
- 删除前弹出明确确认对话框且默认取消；删除成功后仅将对应节点标记为离线缺失，完整保留已有用户标签以便未来恢复。
- 完善文件详情与在线状态面板：结构化呈现名称、完整路径、扩展名、大小、修改时间、在线状态、稳定身份状态、用户标签、自动标签及诊断信息，并提供“复制路径”入口。

### 升级与风险

- 原生兼容 v0.2.0 schema v5 数据库；升级前仍建议导出用户标签 JSON。
- 发布包未签名，仅支持 Windows x64；不包含永久删除、外部 Shell 菜单扩展或图谱视图。

## 0.2.0 — Alpha

### 自动分类

- 按扩展名生成粗粒度类型和具体格式自动标签，并用最多 32 字节文件头识别常见图片、音视频、文档和压缩格式。
- 扩展名与文件头冲突时显示“类型冲突”，不会修改真实文件；读取失败保留扩展名分类且不阻塞索引。
- 新增或元数据变化时自动重算，只替换自动标签；用户标签保持不变，也可手动重新识别单个在线文件。

### 实时一致性

- 使用 FileSystemWatcher 跟踪外部创建、修改、删除、重命名和移动，并在 200 ms 窗口合并事件后回读磁盘状态。
- 同卷重命名、移动和扩展名变化保持文件节点及用户标签，自动格式标签随真实文件状态更新。
- 应用启动时异步执行补偿扫描；监听错误或丢失事件会合并为受控全根恢复扫描。

### 离线与恢复

- 根目录显示在线、离线或恢复中状态以及最近错误。
- 根目录不可访问时保留已有节点和标签；重新上线后自动扫描并恢复监听。
- 恢复扫描前先重建 watcher，扫描期间的新变更仍会进入定向对账，不留下监听空窗。

### 升级与风险

- v0.1.0 数据库会自动迁移到 schema v5；升级前仍建议导出用户标签 JSON，旧版应用不能打开迁移后的数据库。
- 实时监听基于 FileSystemWatcher，并以启动/错误恢复扫描兜底；本版不读取 NTFS USN Journal。
- 发布包未签名，仅支持 Windows x64；仍不提供文件写操作和图谱视图。

## 0.1.0 — Technical Preview

### 标签式文件管理

- 扫描用户选择的根目录并建立本地 SQLite 索引。
- 使用稳定 Windows 文件身份保留同卷重命名和移动后的标签关系。
- 提供虚拟化文件列表、元数据搜索、排序和多选。
- 支持用户标签 CRUD、批量贴标以及 Any/All 组合筛选。

### 文件与数据安全

- 使用系统默认应用打开文件，并通过原生 Windows Shell API 在资源管理器中定位。
- 导出和恢复版本化用户标签 JSON；自动标签和可重建元数据不进入备份。
- 导入前验证格式、版本、大小和结构，数据库写入在单个事务中完成。

### 升级与风险

- 这是首个公开预览版，没有旧版应用升级要求。
- SQLite schema 会自动向前迁移；降级到不支持当前 schema 的版本会被拒绝。
- 发布包未签名，仅支持 Windows x64。使用前请核对 SHA-256。
- 已知限制见 README：没有实时监听、文件写操作和图谱。
