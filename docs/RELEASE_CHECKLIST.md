# v0.1.0 发布验收

验收日期：2026-09-01
目标平台：Windows 10 1809 及以上，x64

## 功能与数据

- [x] 临时目录完成“扫描 → 标签 → 重启后搜索 → 打开 → 导出 → 新数据库恢复”全链路测试。
- [x] 同卷外部重命名后重新扫描，文件节点和用户标签保持不变。
- [x] 用户标签导出与恢复不会覆盖自动标签。
- [x] 10,000 条记录查询返回完整结果，耗时 53.7 ms。
- [x] 10,000 个文件的扫描在后台立即返回（0.0 ms），3.45 s 完成；界面不会被扫描循环阻塞。

## 构建与启动

- [x] Release x64 自包含构建成功。
- [x] 全量 67 项自动化测试通过。
- [x] 开发构建窗口可见且可响应。
- [x] 发布包窗口可见且可响应。
- [x] 发布包包含 `App.xbf`、`MainWindow.xbf` 和 `GuraFile.pri`。
- [x] 文件版本 `0.1.0.0`、产品版本 `0.1.0` 与标签 `v0.1.0` 对齐。
- [x] ZIP 的 SHA-256 与同名 `.sha256` 文件一致；最终值以 Release 附件为准。

## 文档与发布

- [x] README 包含安装、使用、备份、升级和隐私说明。
- [x] `THIRD_PARTY_NOTICES.md` 与许可证文件随发布包分发。
- [x] 已明确 v0.1.0 不提供实时监听、文件写操作和图谱视图。
- [ ] PR CI 与合并后的 `main` CI 通过。
- [ ] 从远程 `main` 的全新克隆再次完成构建、测试、打包和启动检查。
- [ ] GitHub `v0.1.0` Pre-release 已创建，ZIP 与 SHA-256 附件可下载并验证。

最后三项由发布流程完成后在 Issue #7 和 GitHub Release 中记录，避免为发布后的外部状态再次修改发布提交。
